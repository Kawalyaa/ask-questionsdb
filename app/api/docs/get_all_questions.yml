Endpoint for geting all question

tags:
  - Questions
  responses:
    200:
      description: Questions found in the database.
      404:
        description: Questions not found in the database.
      security:
        - JWT:
          description: pass in jwt token ie Bearer <jwt_token>
          type: apiKey
          schema: Bearer
          name: token
          in: header
          BearerFormat: JWT
